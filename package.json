{
  "name": "bean-game",
  "private": true,
  "scripts": {
    "start": "npm --prefix server start",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "18.x"
  }
}